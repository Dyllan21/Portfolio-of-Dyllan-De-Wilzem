import React from 'react'
import ReactDOM from 'react-dom/client'// Link to follow your files to make it work
import App from './App' // To link the tabs to import it in the website of your project

ReactDOM.createRoot(document.getElementById('root')).render(<App /> )
